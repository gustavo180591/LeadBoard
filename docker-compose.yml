version: '3.8'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3002:3000"
      - "5557:5555"
    environment:
      - DATABASE_URL=postgresql://postgres:postgres@db:5437/leadboard
    depends_on:
      - db
    volumes:
      - .:/app
      - /app/node_modules
  db:
    image: postgres:16
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=leadboard
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5437:5432"
volumes:
  pgdata: